services:
  # Main .NET API Service
  - type: web
    name: sparkle-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: ASPNETCORE_URLS
        value: http://+:5000
      - key: ConnectionStrings:DefaultConnection
        fromDatabase:
          name: sparkle-db
          property: connectionString
      - key: JWT__Key
        generateValue: true
      - key: JWT__Issuer
        value: Sparkle
      - key: JWT__Audience
        value: SparkleClient
    
  # PostgreSQL Database (Render's free tier supports PostgreSQL, not SQL Server)
  - type: pserv
    name: sparkle-db
    runtime: postgres
    plan: free
    ipAllowList: []
    postgresSQLVersion: "15"
    initializers:
      - path: ./init-db.sql
